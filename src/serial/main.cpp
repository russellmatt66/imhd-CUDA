int main(int argc, char* argv[]){
    /* Instantiate grid, fluid, and intermediate variables */

    /* Initialize grid, fluid, and intermediate variables */

    /* Write initial conditions out */

    /* SIMULATION LOOP */
    // for (size_t it = 1; it < Nt; it++){
        // Compute intermediate variables (predictor)
        // Update fluid variables (corrector)
        // Write data out
    // }
    return 0;
}